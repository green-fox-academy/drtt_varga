#include <stdio.h>

int main() {
	int i = 53625;
	// tell if it has 11 as a divisor
	if (i % 11 == 0){
        printf("11 is a divisor of %d", i);
	}else {
        printf("11 is NOT a divisor of %d", i);
	}
	return 0;
}
